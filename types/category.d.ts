export type Category = {
    name: string;

    /*
    * Icon of the category
     */
    icon?: string;

    /*
    * Color of the category
     */
    color?: string;

    /*
    * Group id of the group to which the category belongs
    */
    group: string;
}